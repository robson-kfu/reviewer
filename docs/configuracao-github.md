# Propriedades
```yaml
      vcs:
       services:
           github:
               url: "https://api.github.com"
               appId: ${APP_ID}
               installationId: ${CLIENT_SECRET}                 
```